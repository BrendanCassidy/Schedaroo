<?php

include_once 'includes/config.inc';
if (isset($_POST['action'])) {
    switch ($_POST['action']) {
        case "get_notresponded": // combine this with update_problem
          
	  break;

    }
 }

?>